import Loader from "../components/Loader";

export default function Loading() {
  return (
    <Loader
      title="Preparing Authentication..."
      message="Just aligning things for you."
    />
  );
}
