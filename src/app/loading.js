import Loader from "./components/Loader";

export default function Loading() {
  return (
    <Loader
      title="Preparing AjayaaEdTech..."
      message="With you, every step of the way."
    />
  );
}
